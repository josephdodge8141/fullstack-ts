# Local-only dummy values. Copy to .env only when you want to override them.
KEYCLOAK_DB_PASSWORD=local-keycloak-database-password
KEYCLOAK_ADMIN_USERNAME=local-admin
KEYCLOAK_ADMIN_PASSWORD=local-admin-password
SESSION_SECRET=local-development-session-secret-change-before-production
